/**
 * Global loading fallback displayed while server components are being
 * generated.  It provides a basic message that can be reused across
 * all routes.
 */
export default function Loading() {
  return <p>Loading, please wait...</p>;
}