/**
 * Loading state for the `/notes` route.  Displays a message while the
 * server component or data is being fetched.
 */
export default function NotesLoading() {
  return <p>Loading, please wait...</p>;
}